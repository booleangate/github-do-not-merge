name: 'DO NOT MERGE'
description: 'Make PRs with "do not merge" labels or tags unmergable.'
inputs:
  labels:
    - type: 'label'
      description: 'Who to greet'
      required: true
      default: 'World'
runs:
  using: 'node12'
  main: 'dist/index.js'
